<!DOCTYPE html>
<html>

<head>
    <title>Entre no Troca Vagas</title>
</head>

<body>

<br><br><br>
<p class="titulo-entrar">Entre ou crie uma conta para encontrar a vaga desejada.</p>
{{#if errorMessage}}
    <p class="error-message">{{ errorMessage }}</p>
  {{/if}}

 <div class="form-entrar">
    <form action="/auth/login" method="POST">
        <label for="cpf">CPF:  </label>
        <input type="text" id="cpf" name="cpf" required placeholder="Digite seu CPF" maxlength="14">
        <label for="password">Senha:</label>

        <input type="password" id="password" name="password" placeholder="Digite a senha" required><br>

        <button type="submit" class="form-entrar button">Entrar</button>

    </form>
    

  
   <!-- <script>
        window.onload = function () {
            const urlParams = new URLSearchParams(window.location.search);
            const fail = urlParams.get('fail');

            if (fail === 'true') {
                const failMessage = 'Credenciais inválidas. Por favor, tente novamente.';
                document.getElementById('resultado').innerHTML = '<div class="alert alert-danger" role="alert">' + failMessage + '</div>';
            }
        }
    </script>

     Botão para redefinir a senha -->
    <p>
        <a href="/forgot-password">Esqueceu a senha?</a>
    </p>

    <!-- Botão para criar um novo usuário -->
    <p>
        Não tem uma conta? <a href="./register">Criar conta</a>
    </p>

    </div>

    <script>
        window.onload = function () {
            const urlParams = new URLSearchParams(window.location.search);
            const error = urlParams.get('error');
            const success = urlParams.get('success');

            if (error) {
                document.getElementById('resultado').innerText = error;
                document.getElementById('resultado').style.color = 'red';
            } else if (success) {
                document.getElementById('resultado').innerText = success;
                document.getElementById('resultado').style.color = 'green';
            }
        }
    </script>

</body>

</html>